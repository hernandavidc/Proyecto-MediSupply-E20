apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: medisupply
  labels:
    app: order-service
    service: order-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: order-service

