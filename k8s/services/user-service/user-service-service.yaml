apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: medisupply
  labels:
    app: user-service
    service: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: user-service
